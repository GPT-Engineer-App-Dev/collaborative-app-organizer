import { Box, Heading } from "@chakra-ui/react";

const Channel = () => {
  return (
    <Box p={4}>
      <Heading as="h1" size="xl">Channel Page</Heading>
      <p>This is the Channel page where communication channels and their activities are displayed.</p>
    </Box>
  );
};

export default Channel;